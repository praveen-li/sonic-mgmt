diff a/ansible/roles/vm_set/tasks/add_topo.yml b/ansible/roles/vm_set/tasks/add_topo.yml	(rejected hunks)
@@ -4,8 +4,9 @@
     username: "{{ docker_registry_username }}"
     password: "{{ docker_registry_password }}"
     name: ptf_{{ vm_set_name }}
-    image: "{{ docker_registry_host }}/{{ ptf_imagename }}"
-    pull: always
+#    image: "{{ docker_registry_host }}/{{ ptf_imagename }}"
+#    pull: always
+    image: "{{ ptf_imagename }}"
     state: reloaded
     net: none
     detach: True
